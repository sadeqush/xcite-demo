{
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "prisma": "^4.11.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "start": "npm install && npx prisma generate && npx prisma db push && npx prisma db seed && npx ts-node-dev --no-notify --exit-child src"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^4.11.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
